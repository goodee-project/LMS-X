<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="gd.fintech.lms.mapper.TeacherLectureMapper">
	<select id="selectTeacherLectureListByPage" parameterType="java.util.Map" resultType="gd.fintech.lms.vo.Lecture">
	    SELECT
			lecture_no lectureNo, 
			account_id accountId, 
			subject_no subjectNo, 
			teacher_name teacherName, 
			lecture_name lectureName, 
			lecture_startdate lectureStartdate, 
			lecture_enddate lectureEnddate
		FROM lecture
		WHERE account_id = #{accountId}
		ORDER BY lecture_no asc
		LIMIT #{beginRow}, #{rowPerPage}
	</select>
	
	<select id="countTeacherLecture" parameterType="String" resultType="Integer">
  		SELECT COUNT(*)
  		FROM lecture
  		WHERE account_id = #{accountId};
  	</select>
</mapper>